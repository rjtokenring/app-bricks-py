service_id: arduino:genie
name: Gen AI Inference Extensions (Genie) LLM/VLM service
description: |
  Gen AI Inference Extensions (Genie) LLM/VLM service provides an interface to run and manage large language models (LLMs) and vision language models (VLMs) for Arduino applications.
  It's designed to leverage hardware acceleration features of supported platforms, ensuring efficient performance for AI workloads.
category: ai
supported_boards: ["ventunoq"]
supported_model_types: ["genie:*"]